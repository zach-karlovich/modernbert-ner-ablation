# Archived Results (`results/old`)

- `logs/`: archived training and sweep logs (`*.txt`).
- `sweeps/`: hyperparameter sweep outputs and summary tables (`*_config_*.csv`, `*summary*.csv`, `*results*.csv`).
- `legacy_pairs/`: older run artifact pairs (`*.csv` + `*.json`) that are not the canonical root runs.
